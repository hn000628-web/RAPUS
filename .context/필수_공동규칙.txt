@절대금지@
1. [예시코드] 전면 출력금지 = 토큰낭비
2. [임의 판단 예시 부분 코드] 전면 출력금지 = 그 코드 사용못함 =토큰 낭비
※ 모든 코드는 실제 서비스용 코드 아니면 출력금지.
※ 사용자의 요청 아니면 코든 코드는 출력금지 = 작업시간 낭비
=========================
공용 API/공용 컬럼은 위험부담이 낮은 조회/표시/검색 영역에만 사용한다.
주문, 장바구니, 결제, 예약, 정산, 권한처럼 상태 변경·금전·책임 귀속이 발생하는 기능은 반드시 도메인별 전용 API/Service/컬럼명으로 분리한다.

=============
// FILE : docs/CONTEXT_RESOLVE_CHANNELCODE_PROFILEID_OFFICIAL_RULE_SNAPSHOT.txt - [반드시 규칙준수 : 필수]
// ROOT : docs/CONTEXT_RESOLVE_CHANNELCODE_PROFILEID_OFFICIAL_RULE_SNAPSHOT.txt
// STATUS : OFFICIAL LOCK
// ROLE : BUSINESS / GENERAL SINGLE-OWNERSHIP CONTEXT RESOLVE OFFICIAL RULE
// CHANGE SUMMARY :
// - 단일 귀속 프론트/백엔드/서비스/DB 컨텍스트 기본값 공식 고정
// - channelCode + profileId 동시 확정 구조를 기본값으로 고정
// - storage 추정 기반 profileId / channelCode 사용 금지 명문화
// - profiles 테이블 기준 컨텍스트 resolve 구조 공식화
// - 단일 귀속 페이지/API/Service 전 구간 동일 적용 규칙 확정

// SECTION 01 : PURPOSE

/*
본 스냅샷은 단일 귀속 데이터 처리의 공식 기본값을 확정한다.

본 프로젝트의 단일 귀속 컨텍스트는
반드시 profiles 테이블 기준으로 확정해야 하며,
Frontend / API / Service / DB 전 구간에서
channelCode + profileId 쌍을 동일하게 사용한다.

이 스냅샷 이후
단일 귀속 컨텍스트를
storage 추정 / 단독 profileId / 단독 channelCode 기준으로 처리하는 방식은
비공식 구조로 간주한다.
*/


// SECTION 02 : CORE PRINCIPLE

/*
공식 기본값:

1. 단일 귀속 컨텍스트의 원점은 profiles 테이블이다.
2. 단일 귀속 컨텍스트는 반드시 channelCode + profileId 쌍으로 확정한다.
3. channelCode는 외부 식별 / URL / 라우팅 / 공개 조회 기준이다.
4. profileId는 내부 JOIN / relation / FK 보조 키이다.
5. 둘 중 하나만으로 단일 귀속 컨텍스트를 완성했다고 간주하면 안 된다.
6. 컨텍스트 미확정 상태에서 저장 / 수정 / 삭제를 수행하면 안 된다.
*/


// SECTION 03 : IDENTITY ROLE RULE

/*
식별자 역할 정의:

A. channelCode
- 외부 식별자
- URL 기준
- 공개 조회 기준
- 프로필 귀속 루트 키
- immutable

B. profileId
- 내부 식별자
- JOIN / relation / FK 보조 키
- 단독 공개 식별자 아님
- 단독 컨텍스트 기준 사용 금지

결론:
외부 = channelCode
내부 = profileId
단일 귀속 = channelCode + profileId
*/


// SECTION 04 : CONTEXT RESOLVE SOURCE RULE

/*
컨텍스트 resolve 우선순위:

1. profiles 기준 서버 응답
2. 인증 기반 현재 프로필 컨텍스트 API
3. channelCode 기반 detail API 응답
4. 이미 확정된 상위 페이지 state / provider

절대 금지:

- localStorage 에서 profileId 추정
- sessionStorage 에서 profileId 추정
- storage JSON 구조를 파싱해서 profileId 추정
- storage JSON 구조를 파싱해서 channelCode 추정 후 단독 사용
- 화면에 떠 있는 데이터만으로 저장 허용
- URL 없는 상태에서 임의 추론으로 컨텍스트 생성
*/


// SECTION 05 : FRONTEND OFFICIAL RULE

/*
Frontend 공식 규칙:

1. 모든 단일 귀속 페이지는 진입 즉시 현재 컨텍스트를 resolve 해야 한다.
2. resolve 결과는 반드시 아래 구조를 state 로 가진다:

{
  profileId: number
  channelCode: string
}

3. 단일 귀속 페이지의 조회 / 저장 / 삭제 / 수정은
   위 컨텍스트를 기준으로만 수행한다.
4. 버튼 활성/비활성은 UI 상태로 제어 가능하지만
   실제 submit 실행 전에는 반드시 컨텍스트 확정을 재검증해야 한다.
5. 페이지 단독으로 컨텍스트를 만들 수 없으면
   구현을 억지로 진행하지 말고
   컨텍스트 공급 경로를 먼저 연결해야 한다.
6. avatar / hero / gallery / blocks / hours / place / settings 전부 동일 규칙 적용
7. 단일 귀속 페이지는 channelCode 없는 진입을 비정상 상태로 간주한다.
*/


// SECTION 06 : FRONTEND CONTEXT PATTERN

/*
공식 패턴:

STEP 1
현재 단일 귀속 컨텍스트 확보

STEP 2
channelCode + profileId 상태 확정

STEP 3
상세 조회 / relation 조회 / 폼 초기값 반영

STEP 4
저장 / 삭제 / 수정 시
확정된 channelCode + profileId 재사용

표준 흐름 예시:

route
→ current context resolve
→ { channelCode, profileId } state
→ get detail
→ render
→ submit
→ api call
→ reload detail
*/


// SECTION 07 : API OFFICIAL RULE

/*
단일 귀속 API 공식 규칙:

1. Request / Path / Query / Service 입력은
   channelCode + profileId 쌍을 기준으로 설계한다.
2. profileId 단독 조회 API 금지
3. channelCode 단독으로 내부 수정/삭제 확정 금지
4. 읽기 전용 공개 detail 은 channelCode 기준 허용 가능하나
   write / delete / patch / relation 변경은 channelCode + profileId 기준으로 확정한다.
5. response 에서도 필요한 경우
   channelCode + profileId 를 함께 반환해야 한다.
*/


// SECTION 08 : SERVICE OFFICIAL RULE

/*
단일 귀속 Service 공식 규칙:

1. 입력: channelCode + profileId
2. 내부 DB 조회: WHERE channelCode = ? AND id = ?
3. 수정/삭제 동일 기준
4. 컨텍스트 검증 실패 시 즉시 중단
5. profileId 만으로 ownership 확정 금지
6. channelCode 만으로 write 대상 확정 금지

표준 예시:

SELECT ...
FROM profiles
WHERE channelCode = ?
  AND id = ?
LIMIT 1
*/


// SECTION 09 : DB OFFICIAL RULE

/*
DB 공식 규칙:

1. 단일 귀속 relation 테이블은 channelCode + profileId 구조를 따른다.
2. FK 기준:
   - channelCode → profiles(channelCode)
   - profileId → profiles(id)
3. 단일 귀속 relation row 는
   profile 소유성 검증을 위해 channelCode 와 profileId 를 함께 저장한다.
4. UNIQUE(channelCode, profileId) 또는 동등 무결성 보장 구조 권장
5. INDEX 는 channelCode 기준 필수
6. profileId 는 내부 JOIN 보조 용도로 사용한다.
*/


// SECTION 10 : MEDIA DOMAIN RULE

/*
미디어 도메인 공식 규칙:

1. image_assets
   - 파일 자산 저장 담당
   - channelCode 귀속 저장
   - usageType 관리
   - filePath 저장

2. profile_avatars / profile_hero_images / profile_gallery_images
   - 프로필 relation 담당
   - profileId + channelCode + imageAssetId 기반 연결
   - 대표 이미지 / 배너 / 갤러리 의미 부여

결론:
파일 저장 = media domain
프로필 대표 연결 = profile domain
*/


// SECTION 11 : AVATAR PAGE OFFICIAL RULE

/*
아바타 페이지 공식 규칙:

1. 페이지 진입 시 현재 BUSINESS 또는 GENERAL 프로필 컨텍스트 먼저 확정
2. 컨텍스트는 반드시 profiles 기준 응답으로 확정
3. 아바타 조회는 확정된 컨텍스트 기준으로 수행
4. 업로드 성공 후 반환 assetId 를 사용하여
   profile_avatars relation 을 연결
5. 삭제는 relation unlink 기준으로 수행
6. current avatar 반영 후 재조회
7. storage 추정 기반 profileId 사용 금지
8. query param / provider / current-context-api 중 하나는 반드시 존재해야 한다.
*/


// SECTION 12 : DEFAULT IMPLEMENTATION RULE

/*
기본 구현 원칙:

A. 페이지는 컨텍스트를 직접 추정하지 않는다.
B. 상위 레벨에서 이미 확정된 컨텍스트를 받는 구조를 우선한다.
C. 필요 시 전역 current profile context 유틸/API를 둔다.
D. 아래 구조를 프로젝트 기본값으로 고정한다:

getCurrentBusinessContext() / getCurrentGeneralContext()

반환 예시:

{
  profileId: number,
  channelCode: string,
  profileType: 'BUSINESS'
}

또는

{
  profileId: number,
  channelCode: string,
  profileType: 'GENERAL'
}
*/


// SECTION 13 : FORBIDDEN RULE

/*
절대 금지:

1. localStorage profileId 추정
2. sessionStorage profileId 추정
3. JSON cache 파싱 후 profileId 추정
4. channelCode 없는 상태에서 write 시도
5. profileId 없는 상태에서 write 시도
6. 단일 귀속 페이지가 context 없이 standalone 동작하도록 설계
7. ownerChannelCode 같은 변형 키 사용
8. profileId 단독 API 사용
9. 단일 channelCode 만으로 write 대상 확정
10. UI에 값이 떠 있다는 이유로 저장 가능 상태로 간주
*/


// SECTION 14 : REQUIRED CONTEXT CHECKLIST

/*
모든 단일 귀속 페이지/API 실행 전 체크리스트:

[ ] channelCode 존재
[ ] profileId 존재
[ ] 둘 다 profiles 기준으로 resolve 완료
[ ] relation / save / delete 전 재검증
[ ] write 대상 page/service/db 정합성 확인
[ ] context 미확정 시 중단 처리
*/


// SECTION 15 : FAILURE POLICY

/*
컨텍스트 미확정 시 정책:

1. 저장/수정/삭제 실행 금지
2. 임의 추정 금지
3. fallback 추론 금지
4. 누락 상태를 명시적으로 오류 처리
5. 컨텍스트 공급 경로를 먼저 수정

표준 오류 예시:
- channelCode missing
- profileId missing
- business context missing
- profile context resolve failed
*/


// SECTION 16 : OFFICIAL DECLARATION

/*
본 프로젝트의 단일 귀속 기본값은 아래와 같이 공식 고정한다.

단일 귀속 컨텍스트 = profiles 기준 resolve
단일 귀속 식별 = channelCode + profileId
외부 기준 = channelCode
내부 JOIN 기준 = profileId
Frontend / API / Service / DB 전 구간 동일 적용

이 스냅샷 이후
storage 추정 기반 profileId / channelCode 처리,
profileId 단독 기준 처리,
channelCode 단독 write 처리 방식은
공식 구조가 아니며 Production 사용 금지로 간주한다.
*/


// SECTION END
============================================================
// FILE : PROJECT_RULES/CHANNELCODE_PROFILEID_DB_ENFORCEMENT_SNAPSHOT.txt
// ROOT : PROJECT_RULES/CHANNELCODE_PROFILEID_DB_ENFORCEMENT_SNAPSHOT.txt
// STATUS : PRODUCTION LOCK
// ROLE : CHANNELCODE + PROFILEID DB ENFORCEMENT RULE
// CHANGE SUMMARY :
// - DB 스키마 기준으로 profileId 필요 시 channelCode 강제 적용
// - 단일 귀속 테이블 CRUD API/Service/DB/Frontend 일관 적용
// - profileId 불필요 시 channelCode 기본값 저장 가능
// - ownerChannelCode 사용 금지

/* ==================================================
SECTION 01 : CORE PRINCIPLE
================================================== */

1. 모든 단일 귀속 테이블에서
   - profileId 필요 여부를 확인
   - profileId 존재 시 channelCode 필수 저장
   - profileId 불필요 시 channelCode만 저장 가능

2. channelCode는 플랫폼 기본 식별자이며
   profileId 존재 여부와 관계없이 항상 기본값으로 적용

3. ownerChannelCode 컬럼 사용 절대 금지
   - 단일 귀속 테이블에서 channelCode = ownerChannelCode 의미로 통합

/* ==================================================
SECTION 02 : TABLE CLASSIFICATION
================================================== */

단일 귀속 테이블 (profileId 필요 시)
- profiles
- profile_categories
- image_assets
- profile_avatars
- profile_hero_images
- profile_gallery_images

다중 주체/관계 테이블 (role + ChannelCode)
- posts, post_comments, post_engagements
- orders, payments, reservations
- follows, blocks, notifications, reports, audit_logs

/* ==================================================
SECTION 03 : API / SERVICE RULE
================================================== */

단일 귀속 API:
- Request: { profileId, channelCode, ... }
- Response: 단일 귀속 데이터
- 조회/수정/삭제 시 반드시 profileId + channelCode 기준

단일 귀속 Service 함수:
- 입력: profileId + channelCode
- DB 쿼리: WHERE profileId = ? AND channelCode = ?
- INSERT/UPDATE/DELETE 모두 동일 기준

다중 주체 API/Service:
- 입력: role + ChannelCode
- profileId optional (JOIN 용도)

/* ==================================================
SECTION 04 : DB SCHEMA RULE
================================================== */

- 단일 귀속 테이블 컬럼
  - profileId INTEGER (FK 필요 시)
  - channelCode TEXT NOT NULL
- UNIQUE(profileId, channelCode) 권장
- FK 연결
  - profileId → profiles(id)
  - channelCode → profiles(channelCode)
- INDEX: channelCode 기준 필수

/* ==================================================
SECTION 05 : ENFORCEMENT
================================================== */

- Service Layer 검증
  1. profileId 존재 여부 확인
  2. profileId 존재 시 channelCode 필수
  3. INSERT/UPDATE/DELETE 시 필수 적용
- DB Layer
  - UNIQUE + FK 적용
- Frontend
  - 단일 귀속 데이터 조회/저장/삭제 시 profileId + channelCode 기준

/* ==================================================
SECTION 06 : FINAL DECLARATION
================================================== */

- profileId 필요 시 channelCode 강제 적용
- profileId 불필요 시 channelCode 기본값만 저장
- 모든 CRUD API/Service/DB/Frontend 동일 기준
- 단일 귀속 테이블 ownerChannelCode 금지
- 플랫폼 기준 channelCode = 기본 식별자
- 데이터 연결/추적 안정성 확보
===================================================
// FILE : docs/CHANNELCODE_PROFILEID_API_RULE_SNAPSHOT.txt
// ROOT : C:\Users\kjm\social-platform\docs\CHANNELCODE_PROFILEID_API_RULE_SNAPSHOT.txt
// STATUS : PRODUCTION LOCK
// ROLE : CHANNELCODE + PROFILEID API STANDARD
// CHANGE SUMMARY :
// - 단일 귀속 테이블 ownerChannelCode 제거, channelCode 통합
// - 모든 조회/수정/삭제 API channelCode + profileId 기반으로 통일
// - 다중 주체 테이블은 role+ChannelCode 유지
// - Frontend/Backend/Service/DB 전 구간 동일 규칙 적용

/* ==================================================
SECTION 01 : CORE PRINCIPLE
================================================== */

1. 단일 귀속 테이블(channelCode 1개, profileId 1개) 조회/수정/삭제
   → 항상 channelCode + profileId 기준
2. channelCode = ownerChannelCode 의미로 통합
3. role prefix는 다중 주체/관계 테이블만 허용
4. 모든 API/Service/DB/Frontend 동일 기준

/* ==================================================
SECTION 02 : SINGLE OWNERSHIP TABLES
================================================== */

- profiles
- image_assets
- profile_avatars
- profile_hero_images
- profile_gallery_images

RULE:

1. 컬럼명: channelCode
2. ownerChannelCode 컬럼 금지
3. 조회/저장/수정 시 channelCode + profileId 사용
4. FK 연결도 channelCode 기준
5. INDEX channelCode 기준

/* ==================================================
SECTION 03 : MULTI ROLE TABLES
================================================== */

- post_engagements        → actorChannelCode
- post_comments           → authorChannelCode
- posts                   → authorChannelCode
- orders                  → buyerChannelCode / sellerChannelCode
- payments                → payerChannelCode / receiverChannelCode
- reservations            → customerChannelCode / providerChannelCode
- profile_ownerships      → ownerChannelCode / targetChannelCode
- profile_permissions     → ownerChannelCode / targetChannelCode
- follows                 → followerChannelCode / followingChannelCode
- blocks                  → blockerChannelCode / blockedChannelCode
- feed_seen               → viewerChannelCode
- feed_hide_filter        → actorChannelCode
- notifications           → senderChannelCode / receiverChannelCode
- reports                 → reporterChannelCode / targetChannelCode
- audit_logs              → actorChannelCode

RULE:

1. role prefix + ChannelCode 형태
2. profileId 사용 시 내부 JOIN 보조 키로만 허용
3. 단일 channelCode 컬럼 금지
4. 모든 조회/수정/삭제 API channelCode 기준

/* ==================================================
SECTION 04 : API DESIGN RULE
================================================== */

1. 모든 단일 귀속 API:
   - GET / PATCH / DELETE
   - Request: { channelCode, profileId, ... }
   - Response: 단일 귀속 데이터
2. 다중 주체 API:
   - Request: 역할 기준 channelCode
   - Response: 관련 데이터 조회
3. 절대 금지:
   - ownerChannelCode 사용
   - 단일 channelCode로 다자 관계 처리
   - profileId 단독 조회 (channelCode 없는 상태)
4. Frontend/Service/DB 전 구간 일관 적용

/* ==================================================
SECTION 05 : SERVICE LAYER RULE
================================================== */

1. 단일 귀속 Service 함수:
   - 입력: channelCode + profileId
   - 내부 DB 조회: WHERE channelCode=? AND id=?
   - 수정/삭제 동일 기준
2. 다중 주체 Service 함수:
   - 입력: role + ChannelCode
   - 내부 DB JOIN: profileId optional
3. MediaService, AdminService 등 특수 권한 제외, 단일 귀속 규칙 동일
4. channelCode 변경 금지, immutable

/* ==================================================
SECTION 06 : FRONTEND RULE
================================================== */

1. 단일 귀속 데이터 state 저장/라우팅/조회 시 channelCode + profileId 사용
2. channelCode 외 다른 식별자 사용 금지
3. 단일 귀속 relation 삭제 시 channelCode + profileId 기준 unlink only
4. UI / 스타일 / JSX 구조 변경 금지

/* ==================================================
SECTION 07 : DB SCHEMA RULE
================================================== */

1. 단일 귀속 테이블 컬럼명: channelCode
2. UNIQUE(channelCode, profileId) 권장
3. FK 연결: channelCode → profiles(channelCode)
4. INDEX: channelCode 기준 필수
5. 기존 ownerChannelCode 발견 시 migration 필요
6. SQLite/PostgreSQL 호환

/* ==================================================
SECTION 08 : FINAL DECLARATION
================================================== */

- 단일 귀속 테이블: channelCode + profileId 기반 API 전용
- 다중 주체 테이블: role + ChannelCode
- Backend/Service/DB/API/Frontend 전 구간 동일 적용
- ownerChannelCode 절대 사용 금지
- 기존 스키마 migration 필요 시 명시적 변환 수행
- Frontend UI 영향 없음
- 규칙 위반 시 Production 사용 금지

==================================================
END OF SNAPSHOT
==================================================
===============================
기본규칙 
[MODIFY MODE - HARD LOCK]

아래 ORIGINAL CODE는 절대 기준이다.

절대 금지:
- 기존 코드 삭제
- JSX 구조 변경
- 스타일 변경
- return 내부 수정

허용:
- TYPE 필드 추가
- STATE 필드 추가
- load 함수 내부 추가
- save 함수 내부 추가
- return 맨 아래 UI 추가

결과는 반드시:
"기존 코드 + 추가된 코드" 형태

=== ORIGINAL CODE ===
(전체 코드)
==============================================

※채널 코드 규칙 기본값(공식규정 [수정금지])
[channelCode = ownerChannelCode] 
===========================================

profiles.service.ts
↓
DB
===================================================================
☆API적용규칙☆  => 실제 서비스에만 API규칙 적용/ 나머진 전부[http://localhost:4000] 기반으로 개발
※ frontend/lib/adminApi.ts : 어드민(관리자)공용 API
 [금지사항(예시)]
 fetch('/api/admin/users')
 fetch('http://localhost:4000/api/admin/users')

 [필수사항(예시)]
 import {adminFetch} from '@/lib/adminApi'
 adminFetch('/api/admin/users')
===========================================================
※ frontend/lib/
config.ts : process.env.NEXT_PUBLIC_API_URL || 'http://localhost:4000'
api.ts   : 베이스 API /  config.ts         
 -> 기본적으로 수정금지 / 로그인 실패시 무조건 롤백/ 다른 API는 수정가능 
※config.ts -> api.ts -> adminApi.ts/profileApi.ts/regionsApi.ts
수정금지 API : config.ts / api.ts
수정가능 API : adminApi.ts/profileApi.ts/regionsApi.ts/ 기타 등등
===========================================================
adminApi.ts      ← 관리자 API BASE
authApi.ts        ← 로그인
profileApi.ts     ← 프로필
frontend/lib/config.ts      ← URL 관리
frontend/lib/media.ts       ← media helper

 적용 범위 : 
feedApi.ts        ← 피드
postApi.ts        ← 게시글
commentApi.ts
========================================================
AI IMPLEMENTATION SNAPSHOT RULE
VERSION : v2.0
PURPOSE : AI 구현 혼선 방지 / Domain 책임 고정
PRIORITY : ABSOLUTE
========================================================

[0. CORE PRINCIPLE]

DELETE POLICY :

REAL DELETE = ADMIN ONLY

USER DELETE = UNLINK ONLY


========================================================
[1. DOMAIN RESPONSIBILITY]

Business Domain:

Profiles
Posts
Feed

ROLE:

relation management only

ALLOWED:

set assetId
remove assetId
relation delete

FORBIDDEN:

file delete
image_assets delete
fs access
storage access


--------------------------------------------------------

Storage Domain:

MediaService

ROLE:

storage executor

ALLOWED:

file upload
file delete
image_assets insert
image_assets delete

FORBIDDEN:

profile update
post update
business logic


--------------------------------------------------------

Admin Domain:

AdminService

ROLE:

delete authority

ALLOWED:

real delete
orphan delete
storage cleanup

========================================================
[2. DELETE FLOW ABSOLUTE STRUCTURE]

STEP 1

USER DELETE:

ProfilesService:

UPDATE profiles
SET avatarAssetId = NULL


RESULT:

asset becomes orphan


--------------------------------------------------------

STEP 2

ADMIN CLEANUP:

AdminService:

find orphan assets

call:

mediaService.delete(assetId)


--------------------------------------------------------

STEP 3

MediaService execution:

DELETE FROM image_assets

fs.unlink(file)


========================================================
[3. FRONTEND DELETE DEFINITION]

Frontend delete = UI remove only


Example:

delete avatar button

result:

profile.avatarAssetId NULL

file still exists


========================================================
[4. ABSOLUTE FORBIDDEN USER ACTION]

ProfilesService MUST NEVER:

DELETE image_assets

fs.unlink

fs.rm

storage delete

file delete


ALLOWED:

removeAvatarRelation()

removeHeroRelation()

removePostRelation()


========================================================
[5. MEDIA DELETE AUTHORITY RULE]

MediaService can execute delete.

MediaService cannot decide delete.


VALID:

AdminService → MediaService.delete()


INVALID:

ProfilesService → MediaService.delete()


========================================================
[6. ORPHAN DEFINITION]

Orphan asset:

image_assets row exists

AND

no relation exists


Admin only can delete.


========================================================
[7. SAFE DELETE POLICY]

User action never removes file.

Admin action removes file.


User delete:

SAFE

Admin delete:

HARD DELETE


========================================================
[8. AI IMPLEMENTATION CHECK RULE]

If code contains:

DELETE image_assets

OR

fs.unlink


AI MUST VERIFY:

caller domain == Admin


IF NOT:

RULE VIOLATION


========================================================
[9. STORAGE ACCESS RULE]

Only MediaService may:

access uploads folder

access file system

modify image_assets


ProfilesService forbidden.

PostsService forbidden.


========================================================
[10. FINAL STRUCTURE]

Correct architecture:

Controller → Business Service → MediaService → DB/File


NEVER:

Controller → MediaService directly

Business → storage directly


========================================================
[FINAL ABSOLUTE PRINCIPLE]

FILES:

MediaService only

RELATIONS:

Business Domain only

REAL DELETE:

Admin only


========================================================
END OF SNAPSHOT RULE
========================================================